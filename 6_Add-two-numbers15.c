#include<stdio.h>
int main(){
    int a=67, b=84, add;
    printf("Enter two numbers: ");
    scanf("%d%d",&a, &b);
    add=a+b;
    printf("Addition of two numbers: %d", add);
    return 0;
}